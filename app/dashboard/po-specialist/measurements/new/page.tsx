// P&O Specialists get the same measurement form as Doctors — the underlying
// page already checks the caller's role and the API scopes data the same
// way as consultations/discharge.
export { default } from '@/app/dashboard/doctor/measurements/new/page';
